{{- if .Values.service.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "driver-request-handler.fullname" . }}
  labels:
    {{- include "driver-request-handler.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "driver-request-handler.selectorLabels" . | nindent 4 }}
{{- end }}
